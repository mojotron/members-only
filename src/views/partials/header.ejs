<!-- {isAuth: boolean }  -->
<header class="flex items-center justify-center gap-16 w-full mb-8">
  <nav class="w-full flex justify-between items-center">
    <a href="/">
      <h1 class="font-display font-bold text-4xl">Members Only</h1>
    </a>

    <ul class="flex items-center gap-4">
        <% if(isAuth === false) { %>
          <li>
            <a href="/login" class="px-4 py-2 font-display font-bold text-md bg-green-500 text-gray-100 border border-gray-100 hover:border-green-500 hover:bg-gray-100 hover:text-green-500">login</a>
          </li>
          <li>
            <a href="/register" class="px-4 py-2 font-display font-bold text-md bg-green-500 text-gray-100 border border-gray-100 hover:border-green-500 hover:bg-gray-100 hover:text-green-500">register</a>
          </li>
        <% } else { %>
          <li>
            <a href="/">stories</a>
          </li>
          <li>
            <a href="/dashboard">dashboard</a>
          </li>
          <li>
            <a href="/dashboard">logout</a>
          </li>
        <% } %>
    </ul>
  </nav>
</header>