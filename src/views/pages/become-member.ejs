<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script defer type="module" src="/scripts/main.js"></script>
    <title>Members Only | learn session auth basics</title>
  </head>
  <body class="w-full min-h-[100vh] flex justify-center">
    <main
      class="flex flex-col justify-center items-center px-8 pt-16 pb-8 w-full md:w-[800px]"
    >
      <h2 class="font-typewriter text-2xl text-gray-600 mb-8">
        Become a Member
      </h2>

      <form action="/member" method="post" class="flex flex-col justify-center">
        <div class="flex flex-col gap-2 text-md">
          <label class="font-typewriter text-gray-600" for="member-code"
            >Enter secret Code and become Member</label
          >
          <input
            type="text"
            name="memberCode"
            id="member-code"
            class="w-full font-typewriter bg-gray-100 outline-none border-b border-gray-400 px-2 focus:ring focus:ring-green-300"
          />
        </div>
        <button
          type="submit"
          class="mt-8 px-4 py-2 font-typewriter font-bold text-md bg-green-500 text-gray-100 border border-gray-100 hover:border-green-500 hover:bg-gray-100 hover:text-green-500"
        >
          Send Code
        </button>
      </form>

      <a
        href="/dashboard"
        class="my-8 font-typewriter underline text-green-500 hover:text-gray-600"
        >return to dashboard</a
      >

      <%- include('../partials/footer') %>
    </main>
  </body>
</html>
