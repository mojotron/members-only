export type AppUserType = {
  userUid: string;
  firstName: string;
  lastName: string;
  member: boolean;
  createdAt: string;
};

export type UserSignupType = {
  firstName: string;
  lastName: string;
  email: string;
  password: string;
};
