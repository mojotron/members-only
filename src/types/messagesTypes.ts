export type MessageCardType = {
  messageUid: string;
  title: string;
  createdAt: string;
  userUid: string;
};

export type MessageType = {
  messageUid: string;
  text: string;
  title: string;
  createdAt: string;
  userUid: string;
};
